package gui.menu;

import java.io.*;
import java.nio.charset.Charset;
import java.util.ArrayList;


public class Test {
    public static void main(String[] args) {

        /*String fileName = System.getProperty("user.dir") + "/src/gui/menu/test";
        ArrayList<String> lines = new ArrayList<>();
        try ( BufferedReader br = new BufferedReader(new InputStreamReader( new FileInputStream(fileName), Charset.forName("UTF-8")))) {
            String line;
            while ((line = br.readLine()) != null) {
               lines.add(line);
            }
        } catch (IOException e) {
            e.printStackTrace();  //To change body of catch statement use File | Settings | File Templates.
        }
        for(String line : lines) {
            int count = 0;
            line = line.substring(line.indexOf('j'));
            for(String line2 : lines) {
                line2 = line2.substring(line2.indexOf('j'));
                if(line.equals(line2)) {
                   count++;
                }
            }
            if(count > 1) {
              System.out.println(line + " -> " + count);
            }
        }
*/
    }
}
